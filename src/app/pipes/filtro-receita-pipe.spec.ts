import { FiltroReceitaPipe } from './filtro-receita-pipe';

describe('FiltroReceitaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroReceitaPipe();
    expect(pipe).toBeTruthy();
  });
});
